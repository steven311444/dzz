# 免费部署说明

## 你的部署包

- `五年级预习网站_部署包.zip`（5.7MB，解压后即可上传）
- 解压后结构：
  - `index.html`（首页）
  - `wordbank.js`（词库）
  - `英语音频/`（23 段课文音频 + 83 个单词发音）

## 国内能打开吗？

| 平台 | 免费 | 国内访问 | 说明 |
| --- | --- | --- | --- |
| GitHub Pages | 免费 | 经常打不开 | 不推荐 |
| **Vercel** | 免费 | 一般能访问，个别时段慢 | 部署快，推荐 |
| **Netlify** | 免费 | 一般能访问 | 拖拽上传最省事 |
| Cloudflare Pages | 免费 | 一般能访问，速度一般 | 直接上传文件夹 |
| Gitee Pages | 免费 | 快 | 要实名+审核，政策不稳定，备选 |
| 腾讯云 CloudBase | 有免费额度 | 最稳 | 需要注册云账号 |

## 方案 A：Netlify 拖拽（最省事，推荐先试）

1. 浏览器打开 <https://app.netlify.com/drop>
2. 注册/登录（支持 GitHub 账号或邮箱）
3. 解压 zip，把整个文件夹拖进页面
4. 完成后得到 `https://xxx.netlify.app`，把地址存到手机桌面即可

## 方案 B：Vercel（免费，可以让我帮你跑）

1. 打开 <https://vercel.com> 注册登录
2. 在电脑终端进入解压后的文件夹：

```bash
npx vercel --prod
```

按提示登录确认，完成后得到 `https://xxx.vercel.app`。以后更新就再跑一次这条命令。

## 方案 C：Cloudflare Pages（免费）

1. 打开 <https://dash.cloudflare.com> 登录
2. Workers & Pages → Create → Pages → **Direct Upload**
3. 上传解压后的整个文件夹 → 得到 `https://xxx.pages.dev`

## 方案 D：国内最稳（腾讯云 CloudBase）

1. 注册腾讯云，开通"云开发 CloudBase"（有免费额度）
2. 静态网站托管 → 上传 zip 解压后的 `index.html`、`wordbank.js`、`英语音频/`
3. 得到默认域名，国内访问速度快、稳定

## 以后更新网站

- Netlify / Cloudflare：改完本地文件后，重新拖拽/上传整个文件夹
- Vercel：重新运行 `npx vercel --prod`
- 音频或词库有改动时，同步更新 `英语音频/` 和 `wordbank.js` 再上传

> 小提示：如果以后想要自己的域名，再绑定即可（国内域名需要备案）。
